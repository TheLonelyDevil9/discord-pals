# Discord Pals Dependencies
discord.py==2.3.2
openai==1.12.0
python-dotenv==1.0.1
aiohttp==3.9.3
flask==3.1.2
pyyaml==6.0.2

# Monitoring & Metrics
prometheus-client==0.20.0

# Performance (optional but recommended)
orjson==3.9.15

# Security scanning (dev dependencies)
# pip install bandit safety  # Run: bandit -r . && safety check
